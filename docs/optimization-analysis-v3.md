# 第三轮优化分析

> 2026-07-01 — 基于当前代码全扫 + 之前待办汇总

---

## 已做完（不列了）

删除/CSV/占位符校验/爬虫状态/详情弹窗链接+可复制/cebpubservice/关键词框/知识库导入/标书纯文本/AI设置模型预选

---

## 真实待做 — 按改动量排序

### 极小的（5行以内）

| # | 做什么 | 行数 | 为什么没做 |
|---|--------|------|-----------|
| 1 | 标书导出用 `filedialog.asksaveasfilename()` | 3行 | 被其他事打断 |
| 2 | 日期加「近7天」按钮 | 3行 | 同上 |
| 3 | cebpubservice 翻 10 页 (`list_pages=10`) | 改1个数字 | 同上 |

### 小的（10-20行）

| # | 做什么 | 行数 |
|---|--------|------|
| 4 | 爬虫重试 — base.py 请求层加 `retry` 3次 | 10行 |
| 5 | AI 分析追加不覆盖 — `_set_ai_output` 改 `_append_ai_output` | 20行 |

### 中的（半小时级）

| # | 做什么 | 工作量 |
|---|--------|--------|
| 6 | main.py Web 拆分 — 200行 HTML+FastAPI 拆到 `web/app.py` | 30min |
| 7 | AI 错误提示翻译 — 401→"Key无效"，网络超时→"请检查网络" | 20min |

### 大的

| # | 做什么 | 说明 |
|---|--------|------|
| 8 | 取消爬取功能 | 后台线程设 `_cancel_flag`，循环里检查 |
| 9 | ggzy Playwright 关键词搜索 | 已分析，等环境 |

---

## 新发现问题

### 10. cebpubservice 正文全为空

content 字段存的是空串 `""`。不是 bug——详情页是 JS SPA 渲染的，ctbpsp.com 需要浏览器才能真正抓到正文。当前只有列表数据。

**影响**：cebpubservice 的数据只能用于筛选判断，不能用于生成标书（标书需要正文内容）。

### 11. desktop.py 已涨到 1123 行 62 方法

一个文件承载三个 Tab + 源管理 + 知识库 + AI设置 + 导入。不影响运行，但改起来越来越累。

### 12. 数据库缺少"数据来源"列显示

表格里看不到每条数据是从 ggzy 还是 cebpubservice 来的。想只看 cebpubservice 的数据时没法筛。

---

## 优先级排序

```
立即扫掉（10分钟）:
  1. 近7天按钮      3行
  2. cebpubservice翻10页  1个数字
  3. 标书导出对话框   3行

下一步:
  4. AI分析追加不覆盖  20行
  5. 爬虫重试          10行

可做可不做:
  6. main.py Web拆分
  7. AI错误翻译
  8. 数据来源列

大工程:
  9. ggzy Playwright搜索
  10. desktop 拆3文件
```

---

## 不做

统计图表/es/知识图谱/通用爬虫/筛选持久化/日历控件/列宽拖拽/email推 — 均已多方确认不列入开发计划
